// /**
//  * 类型别名
//  */

// type Add = (x: number, y: number, z?: number) => number

// let add: Add = (x, y, z) => y + x + (z || 0)

// add(1, 2)

// interface Lib {
//   (): void,
//   version: number,
// }

// let add2 = (...rest: number[]) => rest.reduce((total, n) => total + n, 0)

// add2(1, 2)