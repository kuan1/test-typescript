// interface Obj {
// 	a: string
// 	b: number
// 	c: string
// }

// type ReadObj = Readonly<Obj>

// type RartObj = Partial<ReadObj>

// type PickStr = Pick<Obj, 'a'>

// type RecordObj = Record<'x' | 'y', Obj>
